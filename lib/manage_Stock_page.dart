import 'package:flutter/material.dart';

class manage_Stock_page extends StatelessWidget {
  const manage_Stock_page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('manage_Stock_page'),
      ),
      body: const Center(
        child: Text('manage_Stock_page'),
      ),
    );
  }


}
