import 'package:flutter/material.dart';

class add_Stock_page extends StatelessWidget {
  const add_Stock_page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('add_Stock_page'),
      ),
      body: const Center(
        child: Text('add_Stock_page'),
      ),
    );
  }


}
