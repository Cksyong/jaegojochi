import 'package:flutter/material.dart';

class stock_Detail_Info extends StatelessWidget {
  const stock_Detail_Info({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('stock_Detail_Info'),
      ),
      body: const Center(
        child: Text('stock_Detail_Info'),
      ),
    );
  }


}
